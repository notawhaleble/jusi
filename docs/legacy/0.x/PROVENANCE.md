# 0.x Provenance

The audited backend snapshot preceding the Jusi 1.0 foundation is:

- repository: `jusi`
- commit: `7c1e1dd7533e078439f371468b08306eaa550ca5`
- branch at audit time: `once_again`
- date recorded: 2026-08-31
- relation to `main`/`v0.1.1`: two commits ahead

The corresponding legacy frontend evidence is:

- repository: sibling `../jusivim`
- commit: `bed0325`
- branch at audit time: `onemoretime`

The foundation sandbox could not initially write Git metadata. The archival tag was subsequently created at the recorded commit:

```text
legacy/0.x-pre-1.0-2026-08-31
```

The same commit is checked out as the detached `/Users/niku/Documents/dev/jusi-0.x` worktree used by the legacy `venv2` environment.

Ignored `.local/` notes were inspected but not copied wholesale into version control. Verified lessons are curated into 1.0 incidents, invariants, and status instead of preserving an unbounded diary.
