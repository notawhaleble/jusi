# Jusi reference source

Run `jusi install-skills` to install this skill with reference source matching
the installed backend. The installer replaces this file in the installed copy
with the exact source path, version and Git commit. Do not guess a checkout path.
